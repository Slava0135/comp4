int err8() {
  int x = 10;
  int y = 1;
  int count;
  for (int i = y; 10 + i <= 2 * 10 - 1; i = i + 3) { // 3 times
    int z = 5;
    for (int j = i; j < x; j = j + 1) { // 9 + 6 + 3
      int z = -5;
      y = y + z;
    }
    count = count + z;
  }

  for (;y > 0;) {
    x = 100;
  }

  for (;count < 0;) {
    x = -100;
  }
}
